import 'package:gap/gap.dart';
import 'package:app/utils/paddings.dart';

class Gaps {
  static const normal = Gap(Paddings.normal);
  static const large = Gap(Paddings.large);
  static const big = Gap(Paddings.big);
  static const small = Gap(Paddings.small);
  static const tiny = Gap(Paddings.tiny);
}
